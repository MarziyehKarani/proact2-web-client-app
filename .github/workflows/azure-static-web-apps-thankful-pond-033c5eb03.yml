name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - master
      - dev
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - master
      - dev

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          production_branch: "master"
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_THANKFUL_POND_033C5EB03 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "" # Api source code path - optional
          output_location: "build" # Built app content directory - optional
        env: 
            REACT_APP_CLIENTID : "40776ba4-353d-4a81-8900-0ef562a40039"
            REACT_APP_SIGNINUSERFLOW : "B2C_1_signin"
            REACT_APP_DOMAIN : "proact2.b2clogin.com"
            REACT_APP_LOGINENDPOINT : "https://proact2.b2clogin.com/proact2.onmicrosoft.com/B2C_1_signin"
            REACT_APP_SCOPES : "https://proact2.onmicrosoft.com/api/access_as_user"
          ###### End of Repository/Build Configurations ######
        env: 
          REACT_APP_CLIENTID : 48cab400-3b9c-4b41-8db9-af142afeece2
          REACT_APP_SIGNINUSERFLOW : B2C_1_EngitelDevProact
          REACT_APP_DOMAIN : devetproactb2c.b2clogin.com
          REACT_APP_LOGINENDPOINT : https://devetproactb2c.b2clogin.com/devetproactb2c.onmicrosoft.com/B2C_1_EngitelDevProact
          REACT_APP_SCOPES : "https://devetproactb2c.onmicrosoft.com/5640d05e-b6cb-4aac-a235-611db24fd6f3/Api.Scope"

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_THANKFUL_POND_033C5EB03 }}
          action: "close"
